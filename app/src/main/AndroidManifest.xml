<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The camera, our window into the world of physical keys. -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The Watcher's voice, a notification. Required for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Permission to consult the master archives. -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- Permission for the Poltergeist to manifest. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <!-- Permission for services to run in the foreground -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Permission for services to run with special use foreground service type -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />


    <application
        android:name=".BarcodeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Barcodencrypt"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <activity
            android:name=".ui.PasswordActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Barcodencrypt" />

        <!-- The Ritual Chamber for scanning. -->
        <activity
            android:name=".ui.ScannerActivity"
            android:theme="@style/Theme.Barcodencrypt" />

        <!-- The Alchemist's Workshop. -->
        <activity
            android:name=".ui.ComposeActivity"
            android:label="@string/compose_message"
            android:theme="@style/Theme.Barcodencrypt" />

        <!-- The Archives. -->
        <activity
            android:name=".ui.ContactDetailActivity"
            android:label="@string/contact_details"
            android:theme="@style/Theme.Barcodencrypt" />

        <!-- A new ghost for the password machine. -->
        <activity
            android:name=".ui.PasswordScannerTrampolineActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- A trampoline for the decryption scanner -->
        <activity
            android:name=".ui.AutofillScannerTrampolineActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- ADDED SettingsActivity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.Barcodencrypt" />

        <activity
            android:name=".ui.OnboardingActivity"
            android:theme="@style/Theme.Barcodencrypt" />

        <!-- The Watcher, forever vigilant. -->
        <service
            android:name=".services.MessageDetectionService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:foregroundServiceType="specialUse"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- The Poltergeist's heart. -->
        <service
            android:name=".services.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse"/>

        <activity
            android:name=".ui.MockMessagesActivity"
            android:label="Mock Conversation"
            android:theme="@style/Theme.Barcodencrypt" />

    </application>

</manifest>